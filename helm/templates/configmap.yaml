apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "fatidentitycat.fullname" . }}
  labels:
    {{- include "fatidentitycat.labels" . | nindent 4 }}
data:
  {{- with .Values.env }}
  {{- toYaml . | nindent 2 }}
  {{- end }}